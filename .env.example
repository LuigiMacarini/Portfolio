# Optional. Without it, the contact form still works but falls back to a
# prefilled mailto: link instead of sending the email directly.
# Needs a sending domain verified in https://resend.com before it will
# deliver to anyone other than the account owner.
RESEND_API_KEY=
